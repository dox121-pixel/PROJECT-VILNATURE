# Content folder — UE5 binary assets tracked via Git LFS
# See .gitattributes for LFS rules.
# Do not manually place files here; use the asset pipeline:
#   python Tools/asset_import_pipeline.py --source /path/to/drop
